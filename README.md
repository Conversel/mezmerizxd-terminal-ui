# mezmerizxd-terminal-ui
 Basic Console Ui in C++

# Setup
* Clone this repository
* Build the project
```bash
./build.sh
```
* Then open the .sln file in Visual Studio
* Use the Visual Studio tools to build/run the project

